<view class="history">
    <view class="container">
        <view class="collect-image">
            <block wx:for="{{collectImg}}">
                <image src="{{item.src}}" data-type="index" data-index="{{index}}" catchtap="getImgInfo"/>
            </block>
        </view>
    </view>
</view>